requests>=2.31
beautifulsoup4>=4.12
# SerpAPI Google Jobs endpoint (indeed.py).
google-search-results>=2.4
# Loads a local .env (SERPAPI_KEY, GEMINI_API_KEY) for dev runs. In CI these
# come from GitHub secrets as real env vars, so the missing .env is a no-op.
python-dotenv>=1.0
# Browser-TLS-impersonating HTTP client for WAF-guarded boards (scrapers/_http.py).
curl_cffi>=0.7
# Headless browser for the few JS-rendered boards (scrapers/_browser.py, HDR).
# CI must also run: playwright install --with-deps chromium
playwright>=1.44
# Note: APPIC turned out to be server-rendered (a session token unlocks the
# results table), so no Playwright/headless browser is required.
